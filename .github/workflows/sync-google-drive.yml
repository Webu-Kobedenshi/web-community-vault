name: Sync Google Drive
on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
jobs:
  rclone:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Convert md to txt
      run: |
        cp -r docs $GITHUB_WORKSPACE/docs-sync
        find $GITHUB_WORKSPACE/docs-sync -name "*.md" | while read f; do
          mv "$f" "${f%.md}.txt"
        done
    - name: rclone
      uses: wei/rclone@v1
      env:
        RCLONE_CONF: ${{ secrets.RCLONE_CONF }}
      with:
        args: sync /github/workspace/docs-sync gdrive:/web-community-vault